---
"cyber-asana": minor
---

Add `createRuntimeContext`, `registerCliCommands`, and `registerMcpTools` for composing a shared Asana client across all domain APIs. Fix CLI and MCP to report the package version from `package.json`.
