---
"cyber-asana": minor
---

Add `task my-tasks` CLI command and `asana_task_my_tasks` MCP tool to list the authenticated user's My Tasks. Supports `--completed-since`, pagination, and `--opt-fields`.
