# Workflows

Cross-capability usage flows — sequences that span more than one domain (e.g. attach a file, then post a status update referencing it). Not a capability itself — no `spec-type` marker. No flows authored yet.
