# Tooling

Build, CI, and packaging concerns: pnpm workspace / turbo build pipeline, changesets release flow, biome lint/format, GitHub Actions CI, dependency updates. Not a capability itself — no `spec-type` marker. No rules authored yet.
