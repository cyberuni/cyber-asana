---
spec-type: behavioral
---

# url

Parse Asana URLs (task/project/portfolio links) into resource type + GID, exposed via the `cyber-asana url` CLI verb and the `asana_url_parse` MCP tool.

## Use Cases

<!-- open: use cases not yet authored — filled in per-unit explore -->
