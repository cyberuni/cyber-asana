{"seq": 1, "kind": "leash", "cr": "global-config-registry", "leash": "auto-all", "by": "derived", "approach": ["additive scenarios only to the frozen config.feature (new --global/--repo/--merged flags on existing verbs, default behavior of every existing verb unchanged)", "new src/global-config.ts module mirrors repo-config.ts's shape (schema_version + entries), keyed by normalized git remote origin URL, no workspace GID or token stored", "merge is local-wins on GID name conflicts, matching the existing explicit-beats-ambient precedence pattern"], "why": "additive spec + code, no narrowing of a frozen scenario, no published-surface break, no new hard floor; repo key + CLI shape + merge precedence were grilled live with the user before drafting"}
{"seq": 2, "kind": "correction", "cr": "global-config-registry", "correction-kind": "judge-iteration", "cause": "coverage-gap", "why": "cold spec-judge found 3 open gaps in the drafted config.feature additions: --merged's repo-key-derivation edge was undocumented/untested, two merged-view scenarios used a one-off step phrasing instead of the established pattern, and resolve-project --merged had no not-found scenario; all three fixed by adding 3 scenarios and rephrasing 2, before re-checking against README.md"}
{"seq": 3, "kind": "gate", "cr": "global-config-registry", "gate": "spec", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "cold spec-judge re-check: every new scenario maps 1:1 to a config/README.md scenario-map row and vice versa, no duplicate edges, purely additive to the frozen feature (git diff shows only appended lines after the original 39 scenarios), and the mermaid/load-bearing-edge prose matches the scenarios verbatim after the fix pass.", "frozen": ["config/config.feature"]}
{"seq": 4, "kind": "gate", "cr": "global-config-registry", "gate": "impl", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "every new scenario has a corresponding unit/CLI test in global-config.test.ts, effective-config.test.ts, or config-cli.test.ts (a few edges, like a scenario's repo-key derivation, are verified at the pure-function level rather than re-verified through the CLI, matching this repo's existing verification style for the frozen suite); pnpm verify green (1387 tests, build, knip) with zero changes to any pre-existing test's expectation."}
