{"seq": 1, "kind": "leash", "cr": "global-user-registry", "leash": "auto-all", "by": "derived", "approach": ["mirror users into the global registry as a flat top-level list (not per-repo, since identity isn't repo-scoped), reusing repo-config.ts's existing addUser/removeUser/removeAliases/resolveUser/observeUser via a synthetic wrapped RepoConfig, same pattern already used for global projects", "add --global/--merged to add-user/resolve-user/remove-user/remove-alias/list-users; extend sync --global to also refresh global users", "add a new resolveEffectiveAssignee (local-then-global) rather than changing the existing repoConfig-only resolveAssignee export, avoiding any breaking change"], "why": "additive spec + code, no narrowing of a frozen scenario (resolveAssignee/--assignee alias resolution was never in config.feature to begin with), no published-surface break, no new hard floor; scope confirmed live with the user before drafting"}
{"seq": 2, "kind": "correction", "cr": "global-user-registry", "correction-kind": "coverage-gap", "cause": "coverage-gap", "why": "cold spec-judge found 3 open gaps: sync --global's project-vs-user repo-filter scoping had no scenario that could distinguish 'refreshed' from 'excluded', the merged-vs-staged contrast for resolve-user had no scenario showing a real cross-scope collision, and the 5 resolveEffectiveAssignee scenarios invented a novel Then-phrasing instead of reusing this file's own 'the outgoing request carries...' family; all three fixed"}
{"seq": 3, "kind": "gate", "cr": "global-user-registry", "gate": "spec", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "cold spec-judge re-check: 17 new scenarios map 1:1 to the two new README scenario-map tables, no duplicate edges, purely additive to the frozen feature (pure append, zero modified lines before it), Given-clause phrasing uniform across all 17, and the flat/no-repo-key + staged-vs-merged design points are each verified by a scenario that actually distinguishes the claimed behavior.", "frozen": ["config/config.feature"]}
{"seq": 4, "kind": "gate", "cr": "global-user-registry", "gate": "impl", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "every new scenario has a corresponding unit test in global-config.test.ts, effective-config.test.ts, config-cli.test.ts, or tasks/cli.test.ts; a real bug was caught during implementation (resolveEffectiveUser accidentally gated the global lookup behind repo-key resolvability, which would have broken global user/assignee resolution outside any git repo) and fixed with a pinning regression test; pnpm verify green (1424 tests, build, knip)."}
